String query = bankHint.toLowerCase(Locale.ROOT);

for (BankDetail bankDetail : bankDataList) {
    if (bankDetail.bankName != null &&
        bankDetail.bankName.toLowerCase(Locale.ROOT).startsWith(query)) {
        matchedBankDetails.add(bankDetail);
    }
}
